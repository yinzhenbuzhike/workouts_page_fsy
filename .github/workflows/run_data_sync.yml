name: Run Data Sync

on:
  workflow_dispatch:
  schedule:
    - cron: '00 1 * * *' # 北京时间09:00运行
    - cron: '30 11 * * *' # 北京时间19:30运行
  push:
    branches:
      - master
    paths:
      - run_page/nike_sync.py
      - run_page/nike_to_strava_sync.py
      - run_page/strava_sync.py
      - run_page/gen_svg.py
      - run_page/garmin_sync.py
      - run_page/coros_sync.py
      - run_page/keep_sync.py
      - run_page/gpx_sync.py
      - run_page/tcx_sync.py
      - run_page/tcx_to_garmin_sync.py
      - run_page/garmin_to_strava_sync.py
      - run_page/keep_to_strava_sync.py
      - run_page/oppo_sync.py
      - requirements.txt

env:
  # please change to your own config.
  RUN_TYPE: keep # support strava/nike/garmin/garmin_cn/keep/only_gpx/only_fit/nike_to_strava/strava_to_garmin/strava_to_garmin_cn/garmin_to_strava/garmin_to_strava_cn/codoon, Please change the 'pass' it to your own
  ATHLETE: 晚风踏星河
  TITLE: 运动记录

  MIN_GRID_DISTANCE: 5 # change min distance here
  TITLE_GRID: 超过5公里的活动 # also here

  # IGNORE_BEFORE_SAVING: True # if you want to ignore some data before saving, set this to True
  IGNORE_START_END_RANGE: 0 # Unit meter
  # Dont making this huge, just picking points you needing. https://developers.google.com/maps/documentation/utilities/polylineutility using this tool to making your polyline
  IGNORE_POLYLINE: ''
  IGNORE_RANGE: 0 # Unit meter
  SAVE_DATA_IN_GITHUB_CACHE: false # if you deploy in the vercel, check the README
  DATA_CACHE_PREFIX: 'track_data'
  BUILD_GH_PAGES: false # If you do not need GitHub Page please set it to `false`
  SAVE_TO_PARQENT: true # If you want to save the data to the repo, set it to `true`
  GENERATE_MONTH_OF_LIFE: true # If you want to generate the month of life, set it to `true`
  BIRTHDAY_MONTH: 1992-10 # If you want to generate the month of life, set it to your birthday month, format is YYYY-MM

jobs:
  sync:
    name: Sync
    runs-on: ubuntu-latest
    outputs:
      SAVE_DATA_IN_GITHUB_CACHE: ${{ steps.set_output.outputs.SAVE_DATA_IN_GITHUB_CACHE }}
      DATA_CACHE_PREFIX: ${{ steps.set_output.outputs.DATA_CACHE_PREFIX }}
      BUILD_GH_PAGES: ${{ steps.set_output.outputs.BUILD_GH_PAGES }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true
      - name: 安装 UV
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
          enable-cache: true
      - name: 缓存 UV 环境
        uses: actions/cache@v3
        id: uv-cache
        with:
          path: |
            ~/.cache/uv
            .venv
            ~/.local/share/uv/python
          key: ${{ runner.os }}-uv-${{ hashFiles('pyproject.toml', 'uv.lock', 'requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-uv-
      - name: 设置 Python
        run: uv python install 3.13

      - name: 安装依赖
        run: |
          if [ -f "pyproject.toml" ]; then
            uv sync --frozen
          elif [ -f "requirements.txt" ]; then
            uv pip install -r requirements.txt
          else
            echo "未找到依赖文件"
          fi

      - name: 显示缓存状态
        run: |
          if [ "${{ steps.uv-cache.outputs.cache-hit }}" == 'true' ]; then
            echo "🎉 UV 缓存完全匹配！依赖安装非常快"
          elif [ -d ".venv" ] || [ -d "~/.cache/uv" ]; then
            echo "🔄 找到之前的缓存！依赖安装较快"
          else
            echo "🔄 未命中缓存，首次安装依赖..."
          fi 
          
      - name: 设置中文语言环境
        run: |
          sudo apt-get update
          sudo apt-get install -y locales
          sudo locale-gen zh_CN.UTF-8
          echo "LANG=zh_CN.UTF-8" | sudo tee /etc/default/locale
          echo "LC_ALL=zh_CN.UTF-8" | sudo tee -a /etc/default/locale
          export LANG=zh_CN.UTF-8
          export LC_ALL=zh_CN.UTF-8

      - name: Cache Data Files
        if: env.SAVE_DATA_IN_GITHUB_CACHE == 'true'
        uses: actions/cache@v4
        with:
          path: |
            activities
            assets
            GPX_OUT
            TCX_OUT
            FIT_OUT
            Workouts
            run_page/data.db
            src/static/activities.json
            imported.json
          key: ${{ env.DATA_CACHE_PREFIX }}-${{ github.sha }}-${{ github.run_id }}
          restore-keys: |
            ${{ env.DATA_CACHE_PREFIX }}-${{ github.sha }}-
            ${{ env.DATA_CACHE_PREFIX }}-

      - name: Run sync Nike script
        if: env.RUN_TYPE == 'nike'
        run: |
          uv run python run_page/nike_sync.py ${{ secrets.NIKE_REFRESH_TOKEN }}

      - name: Run sync Nike to Strava(Run with nike data backup and show with strava)
        if: env.RUN_TYPE == 'nike_to_strava'
        run: |
          uv run python run_page/nike_to_strava_sync.py ${{ secrets.NIKE_REFRESH_TOKEN }} ${{ secrets.STRAVA_CLIENT_ID }} ${{ secrets.STRAVA_CLIENT_SECRET }} ${{ secrets.STRAVA_CLIENT_REFRESH_TOKEN }}

      - name: Run sync Keep script
        if: env.RUN_TYPE == 'keep'
        run: |
          uv run python run_page/keep_sync.py ${{ secrets.KEEP_MOBILE }} ${{ secrets.KEEP_PASSWORD }} --with-gpx

      - name: Run sync Coros script
        if: env.RUN_TYPE == 'coros'
        run: |
          uv run python run_page/coros_sync.py ${{ secrets.COROS_ACCOUNT }} ${{ secrets.COROS_PASSWORD }}
        # If you only want to sync `type running` add args --only-run, default script is to sync all data (rides , swims, strengths trains, hikes, runs, etc.).
        # uv run python run_page/coros_sync.py ${{ secrets.COROS_ACCOUNT }} ${{ secrets.COROS_PASSWORD }} --only-run

      - name: Run sync Keep_to_strava script
        if: env.RUN_TYPE == 'keep_to_strava_sync'
        run: |
          uv run python run_page/keep_to_strava_sync.py ${{ secrets.KEEP_MOBILE }} ${{ secrets.KEEP_PASSWORD }} ${{ secrets.STRAVA_CLIENT_ID }} ${{ secrets.STRAVA_CLIENT_SECRET }} ${{ secrets.STRAVA_CLIENT_REFRESH_TOKEN }} --sync-types running cycling hiking
        # If you only want to sync `type running` modify args --sync-types running, default script is to sync all data (rides, hikes and runs).

      - name: Run sync Strava script
        if: env.RUN_TYPE == 'strava'
        run: |
          uv run python run_page/strava_sync.py ${{ secrets.STRAVA_CLIENT_ID }} ${{ secrets.STRAVA_CLIENT_SECRET }} ${{ secrets.STRAVA_CLIENT_REFRESH_TOKEN }}
        # If you only want to sync `type running` add args --only-run, default script is to sync all data (rides and runs).

      - name: Run sync Codoon script
        if: env.RUN_TYPE == 'codoon'
        run: |
          uv run python run_page/codoon_sync.py ${{ secrets.CODOON_MOBILE }} ${{ secrets.CODOON_PASSWORD }}

      - name: Run sync tcx to Garmin script
        if: env.RUN_TYPE == 'tcx_to_garmin'
        run: |
          # uv run python run_page/tcx_to_garmin_sync.py ${{ secrets.GARMIN_SECRET_STRING }}
          uv run python run_page/tcx_to_garmin_sync.py ${{ secrets.GARMIN_SECRET_STRING_CN }} --is-cn

      # for garmin if you want generate `tcx` you can add --tcx command in the args.
      - name: Run sync Garmin script
        if: env.RUN_TYPE == 'garmin'
        run: |
          # make garmin secret string `uv run python run_page/get_garmin_secret.py ${email} ${password}
          uv run python run_page/garmin_sync.py ${{ secrets.GARMIN_SECRET_STRING }}
        # If you only want to sync `type running` add args --only-run, default script is to sync all data (rides and runs).
        # uv run python run_page/garmin_sync.py ${{ secrets.GARMIN_SECRET_STRING }} --only-run

      - name: Run sync Garmin CN script
        if: env.RUN_TYPE == 'garmin_cn'
        run: |
          # make garmin secret string `uv run python run_page/get_garmin_secret.py ${email} ${password} --is-cn
          uv run python run_page/garmin_sync.py ${{ secrets.GARMIN_SECRET_STRING_CN }} --is-cn --fit
        # If you only want to sync `type running` add args --only-run, default script is to sync all data (rides and runs).
        # uv run python run_page/garmin_sync.py ${{ secrets.GARMIN_SECRET_STRING_CN }}  --only-run --is-cn

      - name: Run sync Garmin CN to Garmin script
        if: env.RUN_TYPE == 'garmin_sync_cn_global'
        run: |
          # make garmin secret string `uv run python run_page/garmin_sync_cn_global.py ${email} ${password} --is-cn
          uv run python run_page/garmin_sync_cn_global.py ${{ secrets.GARMIN_SECRET_STRING_CN }} ${{ secrets.GARMIN_SECRET_STRING }}
        # If you only want to sync `type running` add args --only-run, default script is to sync all data (rides and runs).
        # uv run python run_page/garmin_sync_cn_global.py ${{ secrets.GARMIN_SECRET_STRING_CN }} ${{ secrets.GARMIN_SECRET_STRING }}  --only-run

      - name: Run sync Only GPX script
        if: env.RUN_TYPE == 'only_gpx'
        run: |
          uv run python run_page/gpx_sync.py

      - name: Run sync Only FIT script
        if: env.RUN_TYPE == 'only_fit'
        run: |
          uv run python run_page/fit_sync.py

      - name: Run sync Only TCX script
        if: env.RUN_TYPE == 'only_tcx'
        run: |
          uv run python run_page/tcx_sync.py

      - name: Run sync Strava to Garmin(Run with strava(or others upload to strava) data backup in Garmin)
        if: env.RUN_TYPE == 'strava_to_garmin'
        run: |
          uv run python run_page/strava_to_garmin_sync.py ${{ secrets.STRAVA_CLIENT_ID }} ${{ secrets.STRAVA_CLIENT_SECRET }} ${{ secrets.STRAVA_CLIENT_REFRESH_TOKEN }}  ${{ secrets.GARMIN_SECRET_STRING }} ${{ secrets.STRAVA_EMAIL }} ${{ secrets.STRAVA_PASSWORD }}

      - name: Run sync Strava to Garmin-cn(Run with strava(or others upload to strava) data backup in Garmin-cn)
        if: env.RUN_TYPE == 'strava_to_garmin_cn'
        run: |
          uv run python run_page/strava_to_garmin_sync.py ${{ secrets.STRAVA_CLIENT_ID }} ${{ secrets.STRAVA_CLIENT_SECRET }} ${{ secrets.STRAVA_CLIENT_REFRESH_TOKEN }}  ${{ secrets.GARMIN_SECRET_STRING_CN }} ${{ secrets.STRAVA_EMAIL }} ${{ secrets.STRAVA_PASSWORD }} ${{ secrets.STRAVA_JWT }} --is-cn

      - name: Run sync Garmin-cn to Strava(Run with Garmin data backup in Strava)
        if: env.RUN_TYPE == 'garmin_to_strava_cn'
        run: |
          uv run python run_page/garmin_to_strava_sync.py ${{ secrets.STRAVA_CLIENT_ID }} ${{ secrets.STRAVA_CLIENT_SECRET }} ${{ secrets.STRAVA_CLIENT_REFRESH_TOKEN }}  ${{ secrets.GARMIN_SECRET_STRING_CN }}  --is-cn

      - name: Run sync Garmin to Strava(Run with Garmin data backup in Strava)
        if: env.RUN_TYPE == 'garmin_to_strava'
        run: |
          uv run python run_page/garmin_to_strava_sync.py ${{ secrets.STRAVA_CLIENT_ID }} ${{ secrets.STRAVA_CLIENT_SECRET }} ${{ secrets.STRAVA_CLIENT_REFRESH_TOKEN }}  ${{ secrets.GARMIN_EMAIL }} ${{ secrets.GARMIN_PASSWORD }}

      - name: Run sync Tulipsport script
        if: env.RUN_TYPE == 'tulipsport'
        run: |
          uv run python run_page/tulipsport_sync.py ${{ secrets.TULIPSPORT_TOKEN }} --with-gpx

      - name: Run sync Oppo heytap script, note currently this script is not worked
        if: env.RUN_TYPE == 'oppo'
        run: |
          uv run python run_page/oppo_sync.py ${{ secrets.OPPO_ID }} ${{ secrets.OPPO_CLIENT_SECRET }} ${{ secrets.OPPO_CLIENT_REFRESH_TOKEN }} --with-tcx
        # If you want to sync fit activity in gpx format, please consider the following script:
        # uv run python run_page/oppo_sync.py ${{ secrets.OPPO_ID }} ${{ secrets.OPPO_CLIENT_SECRET }} ${{ secrets.OPPO_CLIENT_REFRESH_TOKEN }} --with-gpx

      - name: Make svg GitHub profile
        if: env.RUN_TYPE != 'pass'
        env:
          TITLE: ${{ env.TITLE }}
          TITLE_GRID: ${{ env.TITLE_GRID }}
          ATHLETE: ${{ env.ATHLETE }}
          MIN_GRID_DISTANCE: ${{ env.MIN_GRID_DISTANCE }}
        run: |
          uv run python run_page/gen_svg.py --from-db --title "$TITLE" --type github --github-style "align-firstday" --athlete "$ATHLETE" --special-distance 20 --special-distance2 40 --special-color yellow --special-color2 red --output assets/github.svg --use-localtime --min-distance 0.01 --background-color "#f5f5f5" --text-color "#006cb8" --track-color "#00AFAA" --empty-data-color "#dedede" --language "zh_CN"

          uv run python run_page/gen_svg.py --from-db --title "超过100公里的活动" --type github --github-style "align-firstday" --athlete "$ATHLETE" --special-distance 120 --special-distance2 140 --special-color yellow --special-color2 red --output assets/github_100.svg --use-localtime --min-distance 100 --background-color "#f5f5f5" --text-color "#006cb8" --track-color "#00AFAA" --empty-data-color "#dedede" --language "zh_CN"

          uv run python run_page/gen_svg.py --from-db --title "$TITLE_GRID" --type grid --athlete "$ATHLETE" --output assets/grid.svg --special-color yellow --special-color2 red --special-distance 20 --special-distance2 40 --use-localtime --min-distance "$MIN_GRID_DISTANCE" --background-color "#f5f5f5" --text-color "#006cb8" --track-color "#00AFAA" --empty-data-color "#dedede" --language "zh_CN"

          uv run python run_page/gen_svg.py --from-db --title "超过100公里的活动" --type grid --athlete "$ATHLETE" --output assets/grid_100.svg --special-color yellow --special-color2 red --special-distance 120 --special-distance2 140 --use-localtime --min-distance 100 --background-color "#f5f5f5" --text-color "#006cb8" --track-color "#00AFAA" --empty-data-color "#dedede" --language "zh_CN"

          uv run python run_page/gen_svg.py --from-db --type circular --use-localtime --background-color "#f5f5f5" --text-color "#006cb8" --track-color "#00AFAA" --empty-data-color "#dedede" --language "zh_CN"
          uv run python run_page/gen_svg.py --from-db --year $(date +"%Y")  --background-color "#f5f5f5" --text-color "#006cb8" --track-color "#00AFAA" --empty-data-color "#dedede" --title "$(date +"%Y") Workouts" --type github --github-style "align-firstday" --athlete "$ATHLETE" --special-distance 20 --special-distance2 40 --special-color yellow --special-color2 red --output assets/github_$(date +"%Y").svg --use-localtime --min-distance 0.01 --background-color "#f5f5f5" --text-color "#006cb8" --track-color "#00AFAA" --empty-data-color "#dedede" --language "zh_CN"
        # if you want use the style:the start day of each year always aligns with Monday. please use the following script:
        # uv run python run_page/gen_svg.py --from-db --title "${{ env.TITLE }}" --type github --github-style "align-monday" --athlete "${{ env.ATHLETE }}" --special-distance 10 --special-distance2 20 --special-color yellow --special-color2 red --output assets/github.svg --use-localtime --min-distance 0.01
        # uv run python run_page/gen_svg.py --from-db --year $(date +"%Y")  --background-color "#f5f5f5" --text-color "#006cb8" --track-color "#00AFAA" --empty-data-color "#dedede" --title "$(date +"%Y") Running" --type github --github-style "align-monday" --athlete "${{ env.ATHLETE }}" --special-distance 10 --special-distance2 20 --special-color yellow --special-color2 red --output assets/github_$(date +"%Y").svg --use-localtime --min-distance 0.01
      - name: Make month of life
        if: env.GENERATE_MONTH_OF_LIFE == 'true'
        env:
          BIRTHDAY_MONTH: ${{ env.BIRTHDAY_MONTH }}
        run: |
          uv run python run_page/gen_svg.py --from-db --type monthoflife --birth "${{env.BIRTHDAY_MONTH}}" --special-color yellow --special-color2 red --special-distance 200 --special-distance2 400 --use-localtime --output assets/mol.svg --use-localtime --athlete "${{env.ATHLETE}}" --title '个人历年汇总' --background-color "#f5f5f5" --text-color "#006cb8" --track-color "#00AFAA" --empty-data-color "#dedede"  --language "zh_CN"

      - name: Save data to parqent
        if: env.SAVE_TO_PARQENT == 'true'
        run: |
          pip install duckdb==1.1.0
          uv run python run_page/save_to_parqent.py

      - name: Push new runs
        if: env.SAVE_DATA_IN_GITHUB_CACHE != 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m 'Add new workouts' || echo "nothing to commit"
          git push || echo "nothing to push"

      - name: Set Output
        id: set_output
        env:
          SAVE_DATA_IN_GITHUB_CACHE: ${{ env.SAVE_DATA_IN_GITHUB_CACHE }}
          DATA_CACHE_PREFIX: ${{ env.DATA_CACHE_PREFIX }}
          BUILD_GH_PAGES: ${{ env.BUILD_GH_PAGES }}
        run: |
          echo "SAVE_DATA_IN_GITHUB_CACHE=$SAVE_DATA_IN_GITHUB_CACHE" >> "$GITHUB_OUTPUT"
          echo "DATA_CACHE_PREFIX=$DATA_CACHE_PREFIX" >> "$GITHUB_OUTPUT"
          echo "BUILD_GH_PAGES=$BUILD_GH_PAGES" >> "$GITHUB_OUTPUT"

# publish_github_pages:
#     if: needs.sync.result == 'success' && needs.sync.outputs.BUILD_GH_PAGES == 'true'
#     name: Build and publish Github Pages
#     uses: ./.github/workflows/gh-pages.yml
#     with:
#       save_data_in_github_cache: ${{needs.sync.outputs.SAVE_DATA_IN_GITHUB_CACHE == 'true'}}
#       data_cache_prefix: ${{needs.sync.outputs.DATA_CACHE_PREFIX}}
#     needs:
#       - sync
